# Simba builds on the host (webpack, babel, loaders) — but those are
# devDependencies, and hosts like Render and Azure set NODE_ENV=production,
# which makes `npm install`/`npm ci` skip devDependencies and breaks
# `npm run build` ("webpack: not found"). Force dev deps to install so the
# production build works on any host.
include=dev
